name: Build and Deploy bo3-multiples-pricing

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install root dependencies
        run: npm install

      - name: Install extension dependencies
        run: cd extensions/bo3-multiples-pricing && npm install

      - name: Install Shopify CLI
        run: npm install -g @shopify/cli@4

      - name: Deploy to Shopify
        env:
          SHOPIFY_APP_AUTOMATION_TOKEN: ${{ secrets.SHOPIFY_APP_AUTOMATION_TOKEN }}
          SHOPIFY_FLAG_STORE: td5h8u-tb.myshopify.com
          SHOPIFY_CLI_TTY: "0"
          NO_COLOR: "1"
          CI: "true"
        run: shopify app deploy --allow-updates --force --no-color
